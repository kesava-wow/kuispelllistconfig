* interface now supports a resizable options frame (e.g. BetterBlizzOptions)
* fix issue where the category doesn't correctly populate upon first load
* colourise the category name. because it's pretty.
